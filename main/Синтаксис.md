# Синтаксис C++

Знание базового синтаксиса C++ является фундаментом для разработки любых программ, от простых консольных приложений до сложных системных программ. В этом разделе мы подробно разберём ключевые элементы синтаксиса, которые помогут вам начать писать эффективный и корректный код на C++.

---

## 📑 Содержание

1. [Структура программы на C++](#структура%20программы%20на%20c)
2. [Переменные и типы данных](#переменные%20и%20типы%20данных)
    - [Основные типы данных](#основные%20типы%20данных)
    - [Модификаторы типов](#модификаторы%20типов)
    - [Инициализация переменных](#инициализация%20переменных)
3. [Операторы](#операторы)
    - [Арифметические операторы](#арифметические%20операторы)
    - [Операторы сравнения](#операторы%20сравнения)
    - [Логические операторы](#логические%20операторы)
    - [Операторы присваивания](#операторы%20присваивания)
    - [Побитовые операторы](#побитовые%20операторы)
4. [Условные операторы](#условные%20операторы)
    - [Оператор `if`](#оператор%20if)
    - [Оператор `switch`](#оператор%20switch)
    - [Тернарный оператор](#тернарный%20оператор)
5. [Циклы](#циклы)
    - [Цикл `for`](#цикл%20for)
    - [Цикл `while`](#цикл%20while)
    - [Цикл `do...while`](#цикл%20dowhile)
    - [Операторы `break` и `continue`](#операторы%20break%20и%20continue)
6. [Функции](#функции)
    - [Объявление и определение функций](#объявление%20и%20определение%20функций)
    - [Передача аргументов](#передача%20аргументов)
    - [Перегрузка функций](#перегрузка%20функций)
7. [Области видимости и время жизни](#области%20видимости%20и%20время%20жизни)
8. [Ввод и вывод данных](#ввод%20и%20вывод%20данных)
9. [Комментарии](#комментарии)
10. [Заключение](#заключение)
11. [Полезные ссылки](#полезные%20ссылки)

---

## Структура программы на C++

Каждая программа на C++ состоит из определённых элементов, необходимых для её корректной работы. Рассмотрим простейшую программу:

```cpp
#include <iostream>  // Подключаем библиотеку для работы с вводом/выводом

int main() {
    std::cout << "Hello, World!" << std::endl;  // Вывод текста на экран
    return 0;  // Возвращаем 0, сигнализируя об успешном завершении программы
}
```

### Разбор программы:

1. **Директива препроцессора `#include`**: Указывает компилятору, что необходимо подключить заголовочный файл. В данном случае `<iostream>` содержит функции для ввода и вывода данных.
2. **Функция `main()`**: Точка входа в программу. С неё начинается выполнение любого приложения на C++.
3. **Оператор вывода `std::cout`**: Используется для вывода информации на стандартный поток вывода (обычно консоль).
4. **Использование пространства имён `std`**: Префикс `std::` указывает, что мы используем функции из стандартного пространства имён.
5. **Оператор `return`**: Завершает функцию `main()` и возвращает код завершения программы. `0` обычно означает успешное выполнение.

---

## Переменные и типы данных

Переменные используются для хранения данных в программе. Каждая переменная имеет тип данных, определяющий, какие значения она может принимать и какие операции над ней можно выполнять.

### Основные типы данных

- **Целочисленные типы**:
    - `short`
    - `int`
    - `long`
    - `long long`
- **Типы с плавающей точкой**:
    - `float`
    - `double`
    - `long double`
- **Символьный тип**:
    - `char`
- **Логический тип**:
    - `bool`
- **Беззнаковые типы**:
    - Добавление модификатора `unsigned` перед целочисленным типом, например: `unsigned int`

### Модификаторы типов

Модификаторы позволяют изменять размер и/или диапазон значений типов данных:

- `signed` и `unsigned`: Указывают, является ли тип знаковым или беззнаковым.
- `short` и `long`: Изменяют размер типа.

Пример:

```cpp
unsigned int positiveNumber = 42;
long long bigNumber = 123456789012345LL;
```

### Инициализация переменных

Переменные можно инициализировать несколькими способами:

- **Классическая инициализация**:

    ```cpp
    int age = 25;
    float height = 1.75f;
    char initial = 'B';
    ```

- **Инициализация в стиле C++11 (uniform initialization)**:

    ```cpp
    int width{1024};
    double pi{3.14159};
    ```

- **Использование ключевого слова `auto`**:

    ```cpp
    auto sum = 10 + 20;  // Компилятор автоматически определит тип переменной как int
    ```

---

## Операторы

Операторы позволяют выполнять операции над данными и переменными.

### Арифметические операторы

- `+`: Сложение.
- `-`: Вычитание.
- `*`: Умножение.
- `/`: Деление.
- `%`: Остаток от деления (для целых чисел).
- `++`: Инкремент (увеличение на 1).
- `--`: Декремент (уменьшение на 1).

Пример:

```cpp
int a = 10;
int b = 3;
int sum = a + b;          // 13
int difference = a - b;   // 7
int product = a * b;      // 30
int quotient = a / b;     // 3
int remainder = a % b;    // 1
a++;  // a становится 11
b--;  // b становится 2
```

### Операторы сравнения

- `==`: Равно.
- `!=`: Не равно.
- `>`: Больше.
- `<`: Меньше.
- `>=`: Больше или равно.
- `<=`: Меньше или равно.

Пример:

```cpp
bool isEqual = (a == b);
bool isGreater = (a > b);
```

### Логические операторы

- `&&`: Логическое "И".
- `||`: Логическое "ИЛИ".
- `!`: Логическое "НЕ".

Пример:

```cpp
bool result = (a > 5) && (b < 10);
```

### Операторы присваивания

- `=`: Присваивание.
- `+=`, `-=`, `*=`, `/=`, `%=`: Сокращённые операции присваивания.

Пример:

```cpp
int x = 5;
x += 3;  // x становится 8
x *= 2;  // x становится 16
```

### Побитовые операторы

- `&`: Побитовое "И".
- `|`: Побитовое "ИЛИ".
- `^`: Побитовое исключающее "ИЛИ" (XOR).
- `~`: Побитовое "НЕ".
- `<<`: Сдвиг влево.
- `>>`: Сдвиг вправо.

Пример:

```cpp
int m = 5;    // 0101 в бинарном виде
int n = 3;    // 0011 в бинарном виде
int andResult = m & n;   // 1 (0001)
int orResult = m | n;    // 7 (0111)
int xorResult = m ^ n;   // 6 (0110)
int notResult = ~m;      // -6 (в двоичном представлении с дополнением до двух)
int shiftLeft = m << 1;  // 10 (1010)
int shiftRight = m >> 1; // 2 (0010)
```

---

## Условные операторы

Условные операторы позволяют выполнять разные блоки кода в зависимости от определённых условий.

### Оператор `if`

```cpp
int x = 10;
if (x > 5) {
    std::cout << "x больше 5" << std::endl;
} else if (x == 5) {
    std::cout << "x равно 5" << std::endl;
} else {
    std::cout << "x меньше 5" << std::endl;
}
```

### Оператор `switch`

```cpp
char grade = 'B';
switch (grade) {
    case 'A':
        std::cout << "Отлично!" << std::endl;
        break;
    case 'B':
        std::cout << "Хорошо!" << std::endl;
        break;
    case 'C':
        std::cout << "Удовлетворительно!" << std::endl;
        break;
    default:
        std::cout << "Попробуйте снова!" << std::endl;
}
```

### Тернарный оператор

Тернарный оператор позволяет записать простое условие в одну строку:

```cpp
int y = 20;
std::string result = (y > 10) ? "y больше 10" : "y меньше или равно 10";
```

---

## Циклы

Циклы используются для повторного выполнения блока кода до тех пор, пока выполняется определённое условие.

### Цикл `for`

```cpp
for (int i = 0; i < 5; i++) {
    std::cout << i << std::endl;  // Выводит числа от 0 до 4
}
```

### Цикл `while`

```cpp
int i = 0;
while (i < 5) {
    std::cout << i << std::endl;
    i++;
}
```

### Цикл `do...while`

```cpp
int i = 0;
do {
    std::cout << i << std::endl;
    i++;
} while (i < 5);
```

### Операторы `break` и `continue`

- `break`: Прерывает выполнение цикла.
- `continue`: Переходит к следующей итерации цикла.

Пример:

```cpp
for (int i = 0; i < 10; i++) {
    if (i == 5)
        continue;  // Пропустить текущую итерацию, когда i равно 5
    if (i == 8)
        break;     // Прервать цикл, когда i равно 8
    std::cout << i << std::endl;
}
```

---

## Функции

Функции позволяют разбивать программу на логические блоки, что упрощает разработку, тестирование и повторное использование кода.

### Объявление и определение функций

```cpp
// Объявление функции (прототип)
int add(int a, int b);

// Определение функции
int add(int a, int b) {
    return a + b;
}

int main() {
    int sum = add(5, 3);
    std::cout << "Сумма: " << sum << std::endl;  // Вывод: Сумма: 8
    return 0;
}
```

### Передача аргументов

- **По значению**: Функция получает копию аргумента.
- **По ссылке**: Функция может изменять оригинальную переменную.
- **По указателю**: Передача адреса переменной.

Пример передачи по ссылке:

```cpp
void increment(int& num) {
    num++;
}

int main() {
    int value = 10;
    increment(value);
    std::cout << value << std::endl;  // Вывод: 11
    return 0;
}
```

### Перегрузка функций

C++ поддерживает перегрузку функций — несколько функций с одним именем, но разными параметрами.

```cpp
int multiply(int a, int b) {
    return a * b;
}

double multiply(double a, double b) {
    return a * b;
}
```

---

## Области видимости и время жизни

- **Локальные переменные**: Объявлены внутри блока `{}` и доступны только внутри него.
- **Глобальные переменные**: Объявлены вне всех функций и доступны в любом месте файла после объявления.
- **Статические переменные**: Объявлены с ключевым словом `static`; сохраняют своё значение между вызовами функции.

Пример:

```cpp
void counter() {
    static int count = 0;
    count++;
    std::cout << "Count: " << count << std::endl;
}

int main() {
    counter();  // Count: 1
    counter();  // Count: 2
    counter();  // Count: 3
    return 0;
}
```

---

## Ввод и вывод данных

Для работы с вводом и выводом используются потоки `std::cin`, `std::cout` и `std::cerr` из заголовочного файла `<iostream>`.

### Вывод данных

```cpp
std::cout << "Введите число: " << std::endl;
```

### Ввод данных

```cpp
int number;
std::cin >> number;
```

Пример полной программы:

```cpp
#include <iostream>

int main() {
    int age;
    std::cout << "Введите ваш возраст: ";
    std::cin >> age;
    std::cout << "Ваш возраст: " << age << std::endl;
    return 0;
}
```

---

## Комментарии

Комментарии используются для документирования кода и игнорируются компилятором.

- **Однострочные комментарии**: Начинаются с `//`.

    ```cpp
    // Это однострочный комментарий
    ```

- **Многострочные комментарии**: Начинаются с `/*` и заканчиваются `*/`.

    ```cpp
    /*
    Это
    многострочный
    комментарий
    */
    ```

---

## Заключение

Теперь вы обладаете более глубоким пониманием базового синтаксиса C++. Это фундаментальные знания, необходимые для дальнейшего изучения языка. Следующим шагом рекомендуется изучить [указатели и ссылки](Указатели%20и%20ссылки.md), что позволит вам работать с памятью и более эффективно управлять данными в программах.

---

## Полезные ссылки

- **[cppreference.com — Справочник по C++](https://en.cppreference.com/w/)**
- **[LearnCpp.com — Учебник по C++](https://www.learncpp.com/)**
- **[Документация по стандартной библиотеке C++](https://en.cppreference.com/w/cpp/header)**

---